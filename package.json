{
  "name": "taxi-booking-system",
  "version": "1.0.0",
  "description": "Full-stack Taxi Booking System (MERN)",
  "scripts": {
    "server": "cd server && node server.js",
    "server:dev": "cd server && npx nodemon server.js",
    "client": "cd client && npm run dev",
    "dev": "npx concurrently \"npm run server:dev\" \"npm run client\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && node seeds/seedStates.js && node seeds/seedCities.js && node seeds/seedFuel.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
